DOMAIN = "nina_polaris"

DEFAULT_PORT = 1888
DEFAULT_SCAN_INTERVAL = 30

CONF_PORT = "port"

# Default cooling target when toggling the camera cooler ON via the switch.
# Users wanting a different target should change it on the NINA side or
# use the camera_cool service directly.
DEFAULT_COOLER_TARGET_TEMP = -10.0

API_BASE_PATH = "/v2/api"
WS_PATH = "/v2/socket"
